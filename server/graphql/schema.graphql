type Movie {
  id: Int!
  title: String!
  rating: Float!
  medium_cover_image: String!
  description_intro: String!
}

type Query {
  movies(limit: Int, rating: Int): [Movie]!
  movie(id: Int!): Movie!
}
